docker

apt update

apt install ca-certificates curl gnupg -y

mkdir -p /etc/apt/keyrings

install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/debian/gpg | \
 gpg --dearmor -o /etc/apt/keyrings/docker.gpg

 chmod a+r /etc/apt/keyrings/docker.gpg

echo \
"deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian \
bookworm stable" | \
 tee /etc/apt/sources.list.d/docker.list > /dev/null

apt update
apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin -y



systemctl stop polarion

ls /opt/polarion

tar -czvf polarion.tar.gz /opt/polarion

mkdir polarion-docker

cd polarion-docker
nano Dockerfile

FROM ubuntu:22.04

ENV DEBIAN_FRONTEND=noninteractive

# Install Java (required)
RUN apt-get update && apt-get install -y \
    openjdk-17-jdk \
    tar \
    && rm -rf /var/lib/apt/lists/*

# Create directory
WORKDIR /opt

# Copy Polarion backup
COPY polarion.tar.gz /opt/

# Extract Polarion
RUN tar -xzvf polarion.tar.gz

# Expose port
EXPOSE 8080

# Start Polarion
CMD ["/bin/bash", "-c", "find /opt -type f -name polarion -path '*/bin/*' | head -n 1 | xargs -I {} {} start"]


ctrl+O
Ctrl+X
enter
mv ~/polarion.tar.gz .

docker build -t polarion-image .


docker run -d \
--name polarion \
-p 8080:8080 \
-v polarion-data:/opt/polarion/data \
polarion-image


docker restart polarion
